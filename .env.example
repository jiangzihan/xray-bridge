# Required: bridge authentication token
BRIDGE_TOKEN=your-secret-bridge-token-here

# Optional: listening port (default 8080)
PORT=8080

# Optional: log level (default info,xray_bridge=debug)
RUST_LOG=info,xray_bridge=debug
